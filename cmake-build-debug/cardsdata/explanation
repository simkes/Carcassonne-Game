у Tile конструктор от Type (возможно потом добавится еще указатель на карточку), texture id = имя файла txt, в карточку тайлы добавляются через Tile( typesvector[number] )  (в game_common.h)
для карточки надо сделать конструктор или оператор ввода из файла
